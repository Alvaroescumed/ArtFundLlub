const carousel = document.querySelector(".imagenes_destacadas");

// creamos un evento al clicar la imagen

carousel.addEventListener('click', ()=>{
    const imageReact = carousel.getBoundingClientRect();
})
